/**
 * src/shared/styles/theme/components/gameSettingsPanel.ts
 * 
 * Style definitions for the game settings panel.
 * This file contains minimal styling as the component now uses Tamagui's built-in styling system.
 * Kept for backwards compatibility with the theme system.
 */

// Define the theme-based styling for the game settings panel
const gameSettingsPanel = {
  // No custom styling needed as we're using Tamagui's built-in components
  // These values can be used by other components that might reference them
  zIndex: 1000,
  animation: {
    duration: 300, // ms
  }
};

export default gameSettingsPanel; 